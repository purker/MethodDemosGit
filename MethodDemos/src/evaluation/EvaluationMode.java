package evaluation;

public enum EvaluationMode
{
	CSV_PER_FILE,
	CSV_PER_PUBLICATIONTYPE,
	CSV_PER_EVALUTATIONTYPE,
	CSV_PER_FILE_AND_EVALUATIONTYPE,

	SYSOUT_DETAILED,
	SYSOUT_SUMMARY;
}
